<script lang="ts">
	// import all available scenes from the file system
	// do not initialize anything until render time is ready
	// only swap to a scene when the animation transition is finished for that scene
	// how does a scene have a "single root transform" for animations?
</script>
